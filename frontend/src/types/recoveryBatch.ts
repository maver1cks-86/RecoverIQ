export interface RecoveryBatch { id:number; merchant_id:number; reference_id:string; source:string; status:string; payment_count:number; revenue_at_risk:string; error_message:string|null; created_at:string; planning_started_at:string|null; planning_completed_at:string|null; execution_started_at:string|null; execution_completed_at:string|null }
export interface BatchConstraints { total_budget:number; incentive_budget:number; max_retries:number; max_contacts:number; max_whatsapp:number; max_incentive_actions:number; max_human_escalations:number; solver_timeout_ms:number; enabled_actions?:string[]|null }
export interface AssignmentAlternative { action:string; recovery_probability:number; expected_value:number; incremental_value:number; intervention_cost:number; incentive_cost:number; rank:number }
export interface PolicyEvidence { action:string; status:string; reason:string }
export interface BatchAssignment { id:number; payment_id:number; amount:string; failure_reason:string|null; selected_action:string; policy_status:string; recovery_probability:number; expected_value:string; incremental_value:string; intervention_cost:string; incentive_cost:string; execution_status:string; provider:string|null; provider_action_id:string|null; provider_status:string|null; payment_url:string|null; standalone_best_action:string|null; selected_rank:number|null; portfolio_agreement:boolean|null; alternatives:AssignmentAlternative[]; policy_evidence:PolicyEvidence[] }
export interface AssignmentPage { items:BatchAssignment[]; page:number; page_size:number; total:number }
export interface BatchSummary { batch:RecoveryBatch; plan_id:number|null; plan_version:number|null; solver_status:string|null; observed_recovered_amount:string; estimated_incremental_value:string; action_allocation:Record<string,number>; execution_counts:Record<string,number>; constraint_utilization:{key:string;used:number;limit:number}[]; constraints_snapshot:Partial<BatchConstraints> }
